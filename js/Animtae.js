$(function () {
    function Animate(element) {
        var key = 'animated';
        var hide = 'hidden';
        var elem = element;
        elem.addClass(key);
        $(elem).removeClass(hide);
    }

 
});